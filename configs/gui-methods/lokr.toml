network_type = "lokr"
network_dim = 8
network_alpha = 8
learning_rate = 2e-5
max_train_epochs = 4
save_every_n_epochs = 2
checkpointing_epochs = 2

use_shuffled_caption_variants = true
cache_llm_adapter_outputs = true
caption_dropout_rate = 0.1
output_name = "anima"

blocks_to_swap = 0
use_cmmd = false

[variant]
family = "lokr"
label = "LOKR"
description = "Low-Rank Kronecker Product. Factorizes dimensions for structured high-rank approximation."
order = 40
