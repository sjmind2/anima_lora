"""Image-to-caption helpers used by editing/inversion paths.

Currently only ``wd_tagger`` (SmilingWolf/wd-swinv2-tagger-v3) — a multi-label
booru-style tagger that produces a comma-separated tag string suitable for
seeding ψ_src in DirectEdit's external-image case.
"""
