"""Image-editing and inversion pipelines (not the core generation engine).

DirectEdit inversion + edit-conditioning swap (``directedit``,
``directedit_splice``, ``edit_dispatcher``) and the postfix-tail inversion probe
(``postfix_inversion``). These are experimental / archived techniques driven by
``scripts/edit.py`` and the comfyui-anima-directedit node, kept out of the
top-level engine namespace.
"""
