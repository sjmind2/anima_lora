{
  "source_image_dir": "원본 학습 이미지가 있는 폴더 — 각각 .txt 캡션 사이드카와 쌍을 이룹니다. 캐싱 단계에서 이 이미지들을 post_image_dataset/resized/로 리사이즈하고 VAE/텍스트/PE 캐시를 post_image_dataset/lora/ 아래에 씁니다. 전처리 전용 설정으로, 학습은 이 값을 읽지 않고 리사이즈 및 캐시된 출력물을 읽습니다. configs/preprocess.toml에 저장되며, 프리셋/메소드 파일로 실행마다 재정의할 수 있고 base.toml에 남아 있는 레거시 값이 우선됩니다 (하위 호환).",
  "drop_lowres_images": "켜면 전처리 중 'Min pixels' 임계값보다 작은 소스 이미지를 건너뛰어 리사이즈 / VAE / 텍스트 캐시에 절대 포함되지 않습니다 — 저해상도 이미지는 학습 신호 기여가 적고 캐시 공간을 낭비합니다. 크기에 상관없이 모든 이미지를 유지하려면 끄세요 (--min_pixels 0 과 동일). configs/preprocess.toml에 저장됩니다.",
  "min_pixels": "저해상도 필터의 픽셀 수 임계값 (가로 × 세로). 500000 = 0.5MP이며 기본값 — 리사이즈/캐싱 스크립트의 argparse 기본값과 일치합니다. '저해상도 이미지 제외'가 켜져 있을 때만 적용되며, 꺼져 있으면 무시됩니다 (필터 비활성). CLI에서 make preprocess ARGS=\"--min_pixels N\" 으로 실행마다 재정의할 수 있습니다. configs/preprocess.toml에 저장됩니다.",
  "caption_shuffle_variants": "텍스트 인코더 캐싱 시 이미지당 생성되는 캡션 변형 수. v0은 원본 캡션 그대로이고, v1..v(N-1)은 스마트 셔플됩니다 (@artist 접두사와 'On the …' / 'In the …' 섹션 앵커는 보존). 데이터로더는 v0을 20% 확률로 선택하고 나머지는 v1..v(N-1) 균등 분포로 선택합니다 — 단, 메소드 설정에 use_shuffled_caption_variants=true 일 때만 적용됩니다. 0으로 설정하면 원본 캡션 하나만 캐싱합니다.",
  "caption_tag_dropout_rate": "v1..v(N-1) 셔플 변형에만 적용되는 태그별 드롭아웃 확률입니다. 첫 번째 @artist 마커까지의 태그는 절대 드롭되지 않습니다 — 작가 태그는 구조적으로 중요하고, 등급/캐릭터/작품 접두사는 순서가 중요합니다. 셔플 변형 수가 0 이하이면 무시됩니다. 일반적: 0.05–0.15. 값이 높을수록 누락된 태그에 강건해지지만 학습 신호가 희석될 수 있습니다.",
  "run_sam_mask": "'마스킹 실행' 시 SAM3 말풍선 분할을 실행할지 여부. 체크 해제 하면 SAM을 완전히 건너뜁니다 — 데이터셋에서 SAM이 오탐을 많이 내거나 MIT의 텍스트 검출만 원할 때 유용합니다. SAM과 MIT 둘 다 해제하면 실행 버튼이 차단됩니다.",
  "run_mit_mask": "'마스킹 실행' 시 MIT/ComicTextDetector 텍스트 분할을 실행할지 여부. 체크 해제하면 MIT를 건너뜁니다 — 만화가 아닌 데이터셋이나 SAM 전용 구성을 빠르게 시험할 때 유용합니다. SAM과 MIT 둘 다 해제하면 실행 버튼이 차단됩니다.",
  "sam_prompts": "SAM3이 각 이미지에서 찾을 텍스트 프롬프트. 한 줄에 하나씩. 기본값은 만화 스타일 말풍선 / 텍스트 영역에 맞춰져 있습니다. 추가로 마스킹하고 싶은 영역이 있으면 커스텀 프롬프트를 추가하세요 (예: 'sound effect', 'caption box'). configs/sam_mask.yaml에 저장되며 preprocess/generate_masks.py가 직접 읽습니다.",
  "sam_focus_prompts": "위 프롬프트와 반대로 동작합니다. 일반 프롬프트는 해당 영역을 마스킹 아웃(손실에서 제외, 나머지 영역으로 학습)합니다. 포커스 프롬프트는 유지할 피사체를 지정합니다. 마스크는 SAM3이 감지한 해당 피사체에만 학습이 적용되고 나머지는 모두 무시됩니다 — 예를 들어 'girl'을 지정하면 배경 전체가 무시되어 LoRA가 캐릭터만 학습합니다. 한 줄에 하나씩. 동일 규칙 안의 일반 프롬프트와 합성됩니다. 최종 학습 영역은 (포커스 피사체) - (무시 영역)이므로, 캐릭터에 포커스하면서도 그 위에 그려진 말풍선은 계속 무시할 수 있습니다. 포커스 피사체가 이미지에서 감지되지 않으면 해당 이미지는 마스크 없이 전체가 학습 대상이 됩니다. 비워두면 기본 무시 전용 동작이 적용됩니다.",
  "sam_rule_path_pattern": "이 규칙이 적용될 이미지를 지정합니다 — post_image_dataset/resized 기준 각 이미지 경로에 대한 fnmatch 글로브. 폴더마다 서로 다른 마스크 유형을 적용할 수 있습니다. 'character_a/*'는 특정 캐릭터를 대상으로 하고, 'char_a/*|char_b/*'는 여러 폴더를 OR 조합하며, '*portrait*'는 경로에 'portrait'이 포함된 모든 파일에 매칭되고, 빈 값 또는 '*'는 모든 이미지에 매칭되는 기본 규칙입니다. 특정 이미지에 패턴이 일치하는 규칙이 여러 개 있으면 모두 합성됩니다(무시 영역은 합집합, 포커스 영역도 합집합). 따라서 좁은 규칙 여러 개와 기본 규칙 하나를 조합하는 구성이 일반적입니다. 상단의 전역 '마스크 경로 필터'(어떤 이미지를 마스킹할지 전체 범위를 제한하며 MIT에도 전달됨)와는 구별됩니다.",
  "sam_threshold": "SAM3 탐지를 유지하기 위한 최소 신뢰도. 범위 0.0–1.0. 값이 낮을수록 더 많은 마스크가 생성되지만 오탐도 늘어나며, 값이 높을수록 엄격해집니다. 기본값 0.5. SAM이 실제 말풍선을 놓치면 값을 낮추고, 관련 없는 영역을 마스킹하면 높이세요.",
  "sam_dilate": "임계값 처리 후 각 SAM 마스크에 적용되는 이진 팽창 픽셀 수. 값이 클수록 마스크 가장자리가 바깥으로 번집니다 — 실제 말풍선 경계보다 분할 결과가 작을 때 유용합니다. 기본값 5. 0으로 비활성화.",
  "mit_text_threshold": "MIT/ComicTextDetector 텍스트 분할기의 신뢰도 임계값. 범위 0.0–1.0. SAM 임계값과는 별개입니다 — MIT는 만화 텍스트 영역에 특화된 다른 모델을 사용합니다. 기본값 0.8. 패널 내부 텍스트를 놓치면 낮추고, 텍스트가 아닌 부분을 잡으면 높이세요.",
  "mit_dilate": "각 MIT 마스크에 적용되는 이진 팽창 픽셀 수. SAM 팽창과 같은 역할이지만 독립적으로 조정 — MIT는 일반적으로 개별 글리프 주변에 타이트한 경계를 분할하므로, 적당한 팽창 값으로 말풍선 단위 블롭으로 합쳐줍니다. 기본값 5.",
  "mask_path_pattern": "마스킹할 리사이즈 이미지를 제한하는 fnmatch glob 패턴. post_image_dataset/resized 기준 각 이미지 경로에 대해 매칭됩니다. GUI에서는 선택한 method 프로필에 저장되며, '마스킹 실행' 시 해당 프로필의 마스크 설정이 작업에 함께 전달되어 SAM과 MIT 백엔드 양쪽이 동일한 범위로 동작합니다. CLI에서 직접 실행할 때는 기존처럼 configs/sam_mask.yaml을 읽습니다. 학습용 path_pattern과 동일한 문법: '*'(또는 빈 값)이면 전체 마스킹; 'char_a/*'이면 한 하위 폴더로 제한; 'char_a/*|char_b/*'으로 여러 폴더를 OR 조합; '*portrait*'이면 경로에 'portrait'이 포함된 모든 파일에 매칭. 전체 마스크 트리를 재생성하지 않고 특정 캐릭터/폴더의 마스크만 (재)생성할 때 사용하세요.",
  "target_res": "다중 스케일 상수 토큰 해상도 티어를 JSON 리스트 형태로 지정 (허용 엣지: 512, 768, 1024, 1280, 1536). [1024]가 기본값 — 단일 ~1MP 티어, 즉 레거시 단일 해상도 동작. 두 개 이상을 나열하면 (예: [1024, 1536]) v1.0 기본 모델처럼 여러 해상도에 걸쳐 학습합니다: 전처리 시 각 이미지는 리사이즈 폭이 가장 작은 티어로 리사이즈되어 (스케일 기준 최근 버킷; 이미지당 출력 하나) ~1MP 이미지는 768로 내려가지 않고 1024를 유지하며 혼합 데이터셋은 자연스럽게 여러 티어에 분산됩니다. 티어별 토큰 수: 512→1024(정사각형)+1008, 768→2160, 1024→4032+4200, 1280→6300, 1536→8640 — 각 포워드는 실제 토큰 수로 실행됩니다 (패딩 없음). 비용은 컴파일된 torch.compile 블록 그래프 수: 768/1280/1536은 각 +1, 512는 +2 (512×512 정사각형과 다양성 패밀리를 포함하므로). 리스트는 짧게 유지하세요. 같은 값은 학습 시에도 torch.compile 캐시 예산 크기를 조정하는 용도로만 읽히며, 학습은 디스크에 이미 캐시된 레이턴트 형상을 그대로 사용합니다."
}
